<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ReminderSaaS.Maui.MainPage">

    <Grid RowDefinitions="*,Auto">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout
                Padding="30,0"
                Spacing="25">
                <Image
                    Source="dotnet_bot.png"
                    HeightRequest="185"
                    Aspect="AspectFit"
                    SemanticProperties.Description="dot net bot in a submarine number ten" />

                <Label
                    Text="Hello, World!"
                    Style="{StaticResource Headline}"
                    SemanticProperties.HeadingLevel="Level1" />

                <Label
                    Text="Welcome to &#10;.NET Multi-platform App UI"
                    Style="{StaticResource SubHeadline}"
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

                <Button
                    x:Name="CounterBtn"
                    Text="Click me" 
                    SemanticProperties.Hint="Counts the number of times you click"
                    Clicked="OnCounterClicked"
                    HorizontalOptions="Fill" />
            </VerticalStackLayout>
        </ScrollView>

        <!-- Fixed Footer with Icons -->
        <Border
            Grid.Row="1"
            Margin="12,8,12,12"
            Padding="0"
            StrokeShape="RoundRectangle 28,28,28,28"
            BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1F1F1F}"
            Stroke="{AppThemeBinding Light=#EFEFEF, Dark=#2A2A2A}"
            StrokeThickness="1"
            HeightRequest="70">

            <Grid ColumnDefinitions="*, *, *" Padding="16" RowSpacing="0" ColumnSpacing="0">
                <!-- Camera Icon Button (Left) -->
                <Button
                    Grid.Column="0"
                    BackgroundColor="Transparent"
                    BorderColor="Transparent"
                    CornerRadius="24"
                    HeightRequest="48"
                    Padding="0"
                    WidthRequest="48"
                    FontSize="32"
                    Text="📷"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"
                    SemanticProperties.Description="Camera"
                    Clicked="OnCameraClicked" />

                <!-- Add Schedule Icon Button (Center) -->
                <Button
                    Grid.Column="1"
                    BackgroundColor="{AppThemeBinding Light=#512BD4, Dark=#ac99ea}"
                    BorderColor="Transparent"
                    CornerRadius="24"
                    HeightRequest="48"
                    Padding="0"
                    WidthRequest="48"
                    FontSize="32"
                    FontAttributes="Bold"
                    Text="+"
                    TextColor="{AppThemeBinding Light=White, Dark=#242424}"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"
                    SemanticProperties.Description="Add Schedule"
                    Clicked="OnAddClicked" />

                <!-- Voice Icon Button (Right) -->
                <Button
                    Grid.Column="2"
                    BackgroundColor="Transparent"
                    BorderColor="Transparent"
                    CornerRadius="24"
                    HeightRequest="48"
                    Padding="0"
                    WidthRequest="48"
                    FontSize="32"
                    Text="🔊"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"
                    SemanticProperties.Description="Voice"
                    Clicked="OnVoiceClicked" />
            </Grid>
        </Border>
    </Grid>

</ContentPage>
